/**
 * @description 合并options
 * @param { Object } innerOptions innerOptions 合并每个组件都需要携带的Options
 * @param { ComponentOptions } options 用户options
 * @param { Component } vm 组件实例
 * @returns Record<string, unknow>
 * 
 */
function mergeOptions(innerOptions: Record<string, unknown>, options: ComponentOptions, vm: Component): Record<string, unknown> {
  return {};
}